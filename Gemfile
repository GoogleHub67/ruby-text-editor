source 'https://rubygems.org'

# Primary framework wrapper for cross-platform desktop windows management
gem 'tk', '~> 0.5.0'

group :development, :test do
  # Code structure formatting and style engine
  gem 'rubocop', require: false
  # Automated test environment framework
  gem 'rspec', '~> 3.12'
end
